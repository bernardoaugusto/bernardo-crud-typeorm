import { typeorm } from '@seidor-cloud-produtos/lib-seidor-common';

export default interface I<%= props.nameCamelCaseUpperFirst %>RequestGetAllDTO extends typeorm.RequestGetAllInterface {<% props.properties.original.strings.forEach((propertie) => { %>
    <%= propertie %>?: string;<%}); %><% props.properties.original.numbers.forEach((propertie) => { %>
    <%= propertie %>?: number;<% }); %>
}
