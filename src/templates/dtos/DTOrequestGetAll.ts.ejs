export default interface I<%= props.nameCamelCaseUpperFirst %>RequestGetAllDTO {<% props.properties.original.strings.forEach((propertie) => { %>
    <%= propertie %>?: string;<%}); %><% props.properties.original.numbers.forEach((propertie) => { %>
    <%= propertie %>?: number;<% }); %>
}
