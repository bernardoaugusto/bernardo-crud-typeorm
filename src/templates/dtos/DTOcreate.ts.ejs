export default interface I<%= props.nameCamelCaseUpperFirst %>CreateDTO {<% props.properties.original.strings.forEach((propertie) => { %>
    <%= propertie %>: string;<%}); %><% props.properties.original.numbers.forEach((propertie) => { %>
    <%= propertie %>: number;<% }); %>
}
